import { HighlighttDirective } from './highlightt.directive';

describe('HighlighttDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlighttDirective();
    expect(directive).toBeTruthy();
  });
});
